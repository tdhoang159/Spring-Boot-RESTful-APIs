spring.application.name=SpringBootRESTfullAPIs
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:springbootrestfulapis}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Bangkok
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:MHung2934!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:truongduchoang}
cloudinary.api-key=${CLOUDINARY_API_KEY:248579782829654}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:sxkpzv4-ePJKtM6PFD6ZUi6FHxE}

# spring.profiles.active=test

# SePay Configuration
sepay.bank-account=${SEPAY_BANK_ACCOUNT:HOANG MANH HUNG}
sepay.bank-account-number=${SEPAY_BANK_ACCOUNT_NUMBER:1056290304}
sepay.bank-short-name=${SEPAY_BANK_SHORT_NAME:VCB}
sepay.payment-prefix=${SEPAY_PAYMENT_PREFIX:TKP1056290304}
sepay.qr-base-url=${SEPAY_QR_BASE_URL:https://qr.sepay.vn/img}
sepay.expire-minutes=${SEPAY_EXPIRE_MINUTES:10}

app.jwt.secret=${APP_JWT_SECRET:ticketbox-attendee-demo-secret-key-must-be-at-least-32-bytes}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}

spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:nqqone@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:xmrh vrck tyyw yunh}
spring.mail.from=${SPRING_MAIL_FROM:nqqone@gmail.com}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
